<h2>Chefs</h2>
{{!-- Create a table --}}
<table id="chefs-table">
    {{!-- Header section --}}
    <thead>

        {{!-- For just the first row, we print each key of the row object as a header cell so we
        know what each column means when the page renders --}}
        <tr>
            {{#each data.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    {{!-- Body section --}}
    <tbody>

        {{!-- For each row, print the id, fname, lname, homeworld and age, in order --}}
        {{#each data}}
        <tr>
            <td>{{this.chef_id}}</td>
            <td>{{this.first_name}}</td>
            <td>{{this.last_name}}</td>
            <td>{{this.email}}</td>
            <td>{{this.restaurant_id}}</td>
            <td><button onclick="deleteChef({{this.chef_id}})">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>
{{!-- <script src="./js/delete_chef.js"></script> --}}

<h2>Adding Data Using an HTML Form</h2>
<p>To add a new chef, please enter their information below and click Submit!</p>
<form id="add-chef-form" method="POST" action="/add-chef-form">
   
    <label for="input-first-name">First Name: </label>
    <input type="text" name="input-first-name" id="input-first-name">

    <label for="input-last-name">Last Name: </label>
    <input type="text" name="input-last-name" id="input-last-name">

    <label for="input-email">Email: </label>
    <input type="text" name="input-email" id="input-email">

    <label for="input-chef-location">Restaurant ID: </label>
    <input type="number" name="input-chef-location" id="input-chef-location">

    <input type="submit">
</form>

<h2>Search Using an HTML Form</h2>
<p>Search for a Chef using the field below. Partial names are OK too!</p>
<form id="search-chef-html" method='GET' action='/chefs'>
    <label for="input-chefs">Search by Chef Name: </label>
    <input type="text" name="chefs" id="input-chefs-html-search">

    <input type="submit">
    <a href='/chefs'><button>Reset</button></a>
</form>